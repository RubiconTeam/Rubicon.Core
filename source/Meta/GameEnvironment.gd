class_name GameEnvironment

## Which type of environment the song's stage will spawn in.

const CANVAS_ITEM : int = 0 ## Spawns a stage that can only display [Node]s deriving from [CanvasItem].
const SPATIAL : int = 1 ## Spawns a stage that can only display [Node]s deriving from [Node3D].